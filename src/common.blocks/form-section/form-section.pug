include ../email-form/email-form.pug
include ../toggle-switch/toggle-switch.pug
include ../tick-box/tick-box.pug

mixin formSection()
  section.form-section
    h2.form-section__title form elements
    .form-section__container
      .form-section__form
        +emailForm()
      .form-section__checkboxes
        form.form-section__toggles
          h2.form-section__title toggles
          .form-section__toggle
            +toggleSwitch()
          .form-section__toggle
            +toggleSwitch({ checked: true })
        form.form-section__tickboxes
          h2.form-section__title tick boxes
          .form-section__tickbox
            +tickBox({ value: '1', name: 'tickbox', checked: true })
          .form-section__tickbox
            +tickBox({ value: '2', name: 'tickbox' })