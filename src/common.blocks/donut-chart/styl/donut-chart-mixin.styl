@import '../../../styles/variables.styl'

setSliceColorByIndex(num)
  index = (num - 1)*1 % 3
  opacityRatio = (num - 1 - index) / 3
  opacity = 1 - 0.2 * opacityRatio
  pie-colors = ( color-text-darker color-orange color-cyan )
  if (opacity > 0)
    return rgba(pie-colors[index], opacity)
