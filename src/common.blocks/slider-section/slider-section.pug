include ../slider/slider.pug

mixin sliderSection()
  section.slider-section
    h2.slider-section__title sliders
    .slider-section__container
      .slider-section__slider
        +slider({ type: 'tip' })
      .slider-section__slider
        +slider({ type: 'scale' })