*, *::after, *::before
	box-sizing border-box
html
	font-size initial-font-size
  background-color white
html, input, textarea
	font-family font-family
	color color-text
h1, h2, h3, p, body
	margin 0
	padding 0
.clearfix
	clear both
body
	overflow-y scroll
	margin 0 auto