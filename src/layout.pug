//- layout.pug

include common.blocks/loader/loader.pug

doctype html
html
	head
		meta(charset = 'utf-8')
		meta(name='viewport', content="inital-scale=1.0, width=device-width")
		title #{title}
	body
		+loader()
		block content